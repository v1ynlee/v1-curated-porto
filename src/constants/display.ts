/**
 * Display limit constants for tag/genre chips.
 *
 * These control how many items are shown on mobile view.
 * Desktop view always shows the full list.
 * Adjust these values to globally change the mobile limits.
 */

/** Maximum number of genre chips displayed on mobile. */
export const MAX_GENRES_SHOW = 2;

/** Maximum number of tag chips displayed on mobile. */
export const MAX_TAGS_SHOW = 1;
